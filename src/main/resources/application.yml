server:
  port: 9000
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring3_hibernate6
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
